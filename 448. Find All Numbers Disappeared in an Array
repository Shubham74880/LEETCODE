class Solution {
public:
    vector<int> findDisappearedNumbers(vector<int>& nums) {
        int n = nums.size();
        vector<int> ans;
        unordered_set<int> freq;
        for(int i = 0; i <n;i++){
            freq.insert(nums[i]);
        }
        for(int i =1;i<=n;i++){
            if(freq.find(i)==freq.end()){
                ans.push_back(i);
            }
        }
        return ans;

    }
};
