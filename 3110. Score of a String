class Solution {
public:
    int scoreOfString(string s) {
    int x = s.size();
    int score = 0;
    for(int i =0;i<x;i++){
        score += abs(int(s[i])-int(s[i+1]));
    }
    return score;
    }
};
